#include <stdio.h>

int main() {
    int secret_value = 101;
    int input;

    printf("Enter a password: ");
    scanf("%d", &input);

    if (input == secret_value) {
        printf("101!\n");
    } else {
        printf("Try pin.\n");
    }

    return 0;
}
